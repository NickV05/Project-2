{{#if user}}

<h2>New Topic</h2>
  <form id = "forumDiv" action="/forum/createTopic" method="POST">
    <label> Topic Name
      <textarea id = "topic"  cols="40" rows="5" name="topicName" placeholder="Adravil side effects" ></textarea>
    </label>
 
    <label> Content
      <textarea id = "content"  cols="40" rows="5" name="content" placeholder="Describe your question"  ></textarea>
    </label>
 
    <button type="submit">Create Topic</button>
 
    {{#if errorMessage}}
    <p class="error">{{errorMessage}}</p>
  {{/if}}
  </form>
  
  {{#each topics}}

    <a href="/forum/details/{{_id}}">
        <p>{{topicName}} added by: {{creator.fullName}}</p>   
    </a>
    <hr>

{{/each}}

{{else}}
  <br><p>Still not logged in user, sorry!</p>
{{/if}}